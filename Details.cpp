#include <bits/stdc++.h>
using namespace std;

// 确定n大小不用difine
const int N = 1e5 + 3; // 加一定的值，方便

// 开全局数组自动初始化
int a[N];

int main()
{
    // 基操：取消同步流
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);


    cout << "Hello world!" << '\n'; // 不用endl

    // 输入和处理分开做会更快（用两个for）
    return 0;
}